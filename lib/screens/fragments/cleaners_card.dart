import 'package:flutter/material.dart';

class CleanersCard extends StatelessWidget {
  const CleanersCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}